# JoyBox
## Features
+ Stream recording
+ Built-in player
+ Push notifications
+ Web interface
## Providers
+ bongacams
+ chaturbate
## How to run
`docker build -t joybox .`

`docker run --name joybox --rm -v $(pwd):/app/data joybox`
